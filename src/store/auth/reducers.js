const initialState = {
    user: null,
    token: null // isAuth = !!token
}

const reducer = (state = initialState, action) => {
    switch (action.type) {
        case 'actiontype...':
            
            break;
    
        default:
            break;
    }

    return state;
};

export default reducer;
